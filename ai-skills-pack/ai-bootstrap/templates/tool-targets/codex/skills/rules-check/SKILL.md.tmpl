---
name: codex-rules-check
description: >-
  Codex 项目规则检查入口。用于在 ${PROJECT_NAME} 开始任务、切换任务、
  修改代码、文档或配置前，先读取 AGENTS.md 和 .codex/rules 下的规则，
  确认适用规则、验证要求和禁止事项。
---

# Codex 规则检查

本 skill 用来弥补 Codex 不会按 Cursor `.mdc` 规则自动触发的差异。它不替代项目宪法，也不替代验证命令；它只负责让 Codex 在执行前主动读取项目规则。

## 使用时机

- 开始处理本项目任务时。
- 切换到新的服务、模块、文档或配置领域时。
- 准备修改代码、文档、规则、skill、hook、构建配置或发布配置前。
- 用户要求检查规则、加载规则、按项目规范执行时。

## 必读入口

1. 先读项目根目录 `AGENTS.md`。
2. 列出 `.codex/rules/*.md`。
3. 按任务类型读取相关规则；无法判断相关性时，先读规则文件标题和 description，再选择需要展开的文件。
4. 如果 `AGENTS.md` 和 `.codex/rules` 表达冲突，暂停并向用户说明冲突位置。

## 执行步骤

1. 用一句话说明本次任务类型。
2. 说明已读取的规则文件。
3. 摘出本次任务必须遵守的 3 到 7 条规则。
4. 确认禁止编辑的路径、需要用户确认的动作和必须执行的验证命令。
5. 再进入方案评审、实现或诊断。

## 输出格式

```text
规则检查：
- 任务类型：<代码 / 文档 / AI 配置 / Git / 其他>
- 已读取：AGENTS.md，.codex/rules/<name>.md
- 本次约束：<关键规则>
- 验证要求：<命令或待确认>
- 冲突 / 缺口：<无 / 具体问题>
```

## 不要做

- 不要把 `.cursor/rules/*.mdc` 当成 Codex 已经自动加载的规则。
- 不要跳过 `AGENTS.md`。
- 不要在没读规则的情况下声称“已按项目规范执行”。
- 不要把规则检查当作验证结果；验证必须通过真实命令或明确说明未执行原因。
