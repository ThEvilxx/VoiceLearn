---
name: ${SKILL_NAME}
description: >-
  ${DESCRIPTION}
  当用户要求运行/执行 "${SKILL_NAME}" 时触发此 skill。
---

# ${SKILL_NAME}

> 由 ai-bootstrap 于 ${TODAY} 自动生成。
> 来源：${SOURCE}
>
> **待人工完善**：
> 1. 优化上方 description，确保能正确触发。
> 2. 用实际工作流替换下方步骤（前置条件、人工确认、错误处理）。
> 3. 完成后删除此待补充说明块。

## 快速执行

```bash
${COMMAND}
```

## 步骤

1. 确认前置条件（环境变量、工作目录、分支）。
2. 执行上述命令。
3. 验证输出 / 产物。
4. 向用户报告执行结果。

## 注意事项

- 此文件以 `SKILL.md.draft` 生成，审阅后重命名为 `SKILL.md`。
