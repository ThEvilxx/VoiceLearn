# ${PROJECT_NAME}

> 由 ai-bootstrap 于 ${TODAY} 生成。预设类型：`go-backend`。
> 这是**草稿**——审阅并自定义后，重命名为 `AGENTS.md`。

## 1. 项目概述

${README_SUMMARY}

- **语言**：Go
- **入口文件**：${ENTRY_FILES}
- **补充说明**：${USER_NOTES}
${EXISTING_CONFIG_NOTE}

## 2. 架构

<!-- 待补充：补充具体的服务架构描述 -->

这是一个 Go 后端项目。常见模式：

- API 层（handler）→ 业务逻辑层（logic）→ 数据层（model）
- RPC 服务 + 服务发现（如适用）
- 数据库访问通过 sqlx / GORM / ent

## 3. 目录规范

${DIRECTORY_TREE}

<!-- 待补充：补充各目录的用途说明 -->

## 4. 核心约定

- **错误处理**：包装错误并附带上下文，禁止静默吞掉错误。
- **日志**：使用结构化日志（logx 或 slog），生产代码禁止 `fmt.Println`。
- **配置**：所有配置通过 `etc/` 下的 YAML 文件管理，密钥通过环境变量注入，禁止提交。
- **测试**：优先使用表驱动测试，测试文件与实现文件放在同一目录。

## 5. 人工智能协作规则

- 编辑前必须先读相关代码。
- 先写计划再编码（≥ 3 个文件的改动必须先写计划）。
- 禁止未经确认就覆盖配置文件或数据库迁移文件。
- 提交信息格式：`type(scope): 描述`（遵循约定式提交）。
- 遇到歧义时先问，不要假设；如有多种方案，展示取舍后让用户选。
- 只改需求相关的代码——diff 里每一行都要能追溯到用户的要求。
- 只清理自己引入的废代码；发现的无关问题留注释提醒，不擅自修改。
- 能简单就不复杂——没被要求的抽象、灵活性、配置化不要加。

## 6. 禁令

- 禁止提交 `.env`、凭证或密钥。
- 禁止在 handler / library 代码中使用 `panic`。
- 禁止无注释使用 `//nolint` 绕过 linter 告警。
