---
description: Go 代码风格规范 —— 对所有 Go 文件生效
globs: "**/*.go"
---

# Go 代码风格

## 命名

- 导出名：PascalCase
- 未导出名：camelCase
- 缩写词：`HTTPClient` 而非 `HttpClient`
- 接口命名：不加 `I` 前缀，使用动词-er 模式（`Reader`、`Handler`）

## 错误处理

- 始终处理错误，除非有明确理由否则禁止用 `_` 忽略 error 返回值。
- 包装错误并附带上下文：`fmt.Errorf("fetch user %d: %w", id, err)`
- 尽早 return 错误，避免深层嵌套。

## 函数

- 函数体 ≤ 80 行，逻辑复杂时拆分子函数。
- 命名返回值仅在提升可读性时使用，不要每个函数都用。
- 优先使用表驱动测试。

## 导入

- 分组：标准库 → 第三方 → 内部包。使用 goimports 自动排序。

## 日志

- 使用结构化日志（logx / slog），包含请求级字段。
- 生产代码禁止使用 `fmt.Println` 或 `log.Println`。
