---
description: 提交信息格式 —— 遵循约定式提交
alwaysApply: true
---

# 提交规范

使用约定式提交格式：

```
type(scope): 简短描述

[可选正文]

[可选尾注]
```

## 类型

| 类型 | 使用场景 |
|---|---|
| `feat` | 新功能 |
| `fix` | Bug 修复 |
| `refactor` | 既不修 bug 也不加功能的代码改动 |
| `docs` | 仅文档修改 |
| `test` | 添加或更新测试 |
| `chore` | 工具、CI、依赖变更 |
| `perf` | 性能优化 |

## 规则

- 标题行 ≤ 72 个字符
- 描述使用简短动词短语，说明本次提交做了什么。
- 作用域可选但推荐使用：`feat(auth): 增加刷新令牌`
- 破坏性变更：类型后加 `!`，例如 `feat(api)!: 调整响应格式`
