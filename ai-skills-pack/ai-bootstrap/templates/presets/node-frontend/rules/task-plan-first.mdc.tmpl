---
description: 先写计划再编码 —— 中等以上改动必须先有计划
alwaysApply: true
---

# 先计划后编码

对中等及以上改动（≥ 3 个文件），**必须**先做以下步骤：

1. **分析** —— 理解需求，阅读相关文档
2. **写计划** —— 创建计划文件或在对话中写简要清单
3. **确认** —— 向用户展示计划，等待批准
4. **编码** —— 只有计划被批准后才开始写代码

## 何时需要写计划文件

- **小改动（< 3 个文件）**：对话中写简要清单即可
- **中等及以上（≥ 3 个文件或新功能）**：必须创建计划文档

## 计划结构

```markdown
# 功能名称

## 背景
为什么需要这个改动。

## 范围
影响哪些文件 / 模块。

## 步骤
1. 第一步（附目标文件路径） → 验证：<如何确认这步做对了>
2. 第二步 → 验证：<如何确认>
3. ...

## 风险
- 已知风险或依赖
```

## 目标驱动原则

把命令式任务转化为可验证的目标：

| 别这样说… | 应该这样说… |
|---|---|
| "加个校验" | "写出非法输入的测试用例，让它们通过" |
| "修这个 bug" | "写一个复现测试，让它从红变绿" |
| "重构 X" | "重构前后测试全过，行为不变" |

每一步都有验证条件，AI 助手才能**自主循环**直到完成。

## 反模式

- "这很简单，不需要计划" —— 如果 ≥ 3 个文件，就写计划。
- "做完再补计划" —— 那是文档，不是计划。
