---
description: 前端组件与代码风格规范
globs: "**/*.{tsx,jsx,vue,svelte,ts,js}"
---

# 组件风格

## 命名

- 组件：PascalCase（`UserCard.tsx`）
- Hooks / composables：camelCase + `use` 前缀（`useAuth.ts`）
- 工具函数：camelCase（`formatDate.ts`）
- 常量：UPPER_SNAKE_CASE

## 组件规范

- 每个文件一个组件。
- Props 接口定义在组件上方，命名为 `{Component}Props`。
- 优先使用函数组件（React）或 `<script setup>`（Vue 3）。
- 可复用逻辑抽取为 hooks / composables。

## TypeScript

- 开启严格模式，避免 `any`——使用 `unknown` 并进行类型收窄。
- 使用类型导入：`import type { Foo } from './types'`。

## 样式

- 选定一种方案，全项目保持一致。
- 除一次性覆盖外，禁止使用内联样式。
- 颜色、间距等设计 token 应来自共享配置。

## 状态管理

- 组件本地状态就保留在本地，只在兄弟组件需要共享时才提升。
- 全局状态仅用于跨切面关注点（鉴权、主题、国际化）。
