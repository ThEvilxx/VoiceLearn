# ${PROJECT_NAME}

> 由 ai-bootstrap 于 ${TODAY} 生成。预设类型：`node-frontend`。
> 这是**草稿**——审阅并自定义后，重命名为 `AGENTS.md`。

## 1. 项目概述

${README_SUMMARY}

- **语言**：TypeScript / JavaScript
- **入口文件**：${ENTRY_FILES}
- **补充说明**：${USER_NOTES}
${EXISTING_CONFIG_NOTE}

## 2. 架构

<!-- 待补充：补充具体的前端架构描述 -->

这是一个 Node / 前端项目。常见模式：

- 基于组件的 UI（React / Vue / Svelte）
- 状态管理（Redux / Zustand / Pinia / Vuex）
- 接口调用通过 fetch / axios / react-query

## 3. 目录规范

${DIRECTORY_TREE}

<!-- 待补充：补充各目录的用途说明 -->

## 4. 核心约定

- **组件**：每文件一个组件，PascalCase 命名。
- **样式**：选定一种方案（CSS Modules / Tailwind / styled-components），全项目保持一致。
- **类型**：优先使用 TypeScript 严格模式，避免 `any`。
- **测试**：测试文件与组件同目录（`*.test.tsx`）或放在 `__tests__/` 目录。

## 5. 人工智能协作规则

- 编辑前必须先读相关组件 / hooks。
- 先写计划再编码（≥ 3 个文件的改动必须先写计划）。
- 禁止手动修改 `package-lock.json` / `yarn.lock`。
- 提交信息格式：`type(scope): 描述`（遵循约定式提交）。
- 遇到歧义时先问，不要假设；如有多种方案，展示取舍后让用户选。
- 只改需求相关的代码——diff 里每一行都要能追溯到用户的要求。
- 只清理自己引入的废代码；发现的无关问题留注释提醒，不擅自修改。
- 能简单就不复杂——没被要求的抽象、灵活性、配置化不要加。

## 6. 禁令

- 禁止提交 `.env.local`、API 密钥或凭证。
- 禁止无注释禁用 ESLint 规则。
- 禁止在一次性覆盖之外使用内联样式。
