# ${PROJECT_NAME}

> 由 ai-bootstrap 于 ${TODAY} 生成。预设类型：`generic-docs`。
> 这是**草稿**——审阅并自定义后，重命名为 `AGENTS.md`。

## 1. 项目概述

${README_SUMMARY}

- **类型**：通用项目 / 文档 / 多语言
- **入口文件**：${ENTRY_FILES}
- **补充说明**：${USER_NOTES}
${EXISTING_CONFIG_NOTE}

## 2. 项目结构

${DIRECTORY_TREE}

## 3. 核心约定

- 代码变更时同步更新相关文档。
- 使用统一的格式化工具（Markdown lint / Prettier / editorconfig）。
- 提交信息格式：`type(scope): 描述`（遵循约定式提交）。

## 4. 人工智能协作规则

- 编辑前必须先读相关文件。
- 先写计划再编码（≥ 3 个文件的改动必须先写计划）。
- 禁止未经确认就覆盖已有内容。
- 遇到歧义时先问，不要假设；如有多种方案，展示取舍后让用户选。
- 只改需求相关的内容——diff 里每一行都要能追溯到用户的要求。
- 只清理自己引入的废内容；发现的无关问题留注释提醒，不擅自修改。
- 能简单就不复杂——没被要求的结构重组、格式变更不要做。

## 5. 禁令

- 禁止提交密钥、凭证或 API 密钥。
- 禁止生成二进制文件或超长哈希。
