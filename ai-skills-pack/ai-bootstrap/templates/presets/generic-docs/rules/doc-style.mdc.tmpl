---
description: 文档写作规范
globs: "**/*.md"
---

# 文档风格

## 格式

- 使用 ATX 风格标题（`#`、`##`、`###`）。
- 每句一行（语义换行），便于 diff 查看。
- 代码块使用围栏格式并标注语言。

## 结构

- 每篇文档以一级标题开头，与文件名一致。
- 超过 100 行的文档建议加目录。
- 每段落 ≤ 5 句话。

## 链接

- 引用仓库内其他文档时优先使用相对链接。
- 外部链接需附标题，定期检查链接是否失效。

## 图片

- 存放在 `docs/assets/` 或 `images/`。
- 必须包含 alt 文本。
- 图表优先使用 SVG，截图使用 PNG。
