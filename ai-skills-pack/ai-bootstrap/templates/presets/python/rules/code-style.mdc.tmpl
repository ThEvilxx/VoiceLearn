---
description: Python 代码风格规范 —— 对所有 Python 文件生效
globs: "**/*.py"
---

# Python 代码风格

## 格式化

- 遵循 PEP 8（通过 ruff 或 black 执行），行长 ≤ 120。
- 使用 isort 或 ruff 进行导入排序。

## 命名

- 函数 / 变量：`snake_case`
- 类：`PascalCase`
- 常量：`UPPER_SNAKE_CASE`
- 私有：以 `_` 前缀标识

## 类型标注

- 所有函数签名必须有类型标注。
- 使用 `from __future__ import annotations` 处理前向引用。
- 在 CI 中通过 mypy 或 pyright 强制检查。

## 错误处理

- 禁止使用裸 `except:` —— 必须指定异常类型。
- 为领域错误定义自定义异常类。
- 重新抛出前先记录异常及上下文。

## 测试

- 使用 pytest，优先使用 fixture 而非 setup/teardown。
- 测试文件命名：`test_<模块>.py`，或放在 `tests/` 目录下。
- 核心逻辑覆盖率 ≥ 80%。

## 导入

- 分组：标准库 → 第三方 → 本地。各组之间空一行。
- 优先使用绝对导入。
