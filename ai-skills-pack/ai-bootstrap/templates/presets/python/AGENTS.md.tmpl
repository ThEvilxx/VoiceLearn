# ${PROJECT_NAME}

> 由 ai-bootstrap 于 ${TODAY} 生成。预设类型：`python`。
> 这是**草稿**——审阅并自定义后，重命名为 `AGENTS.md`。

## 1. 项目概述

${README_SUMMARY}

- **语言**：Python
- **入口文件**：${ENTRY_FILES}
- **补充说明**：${USER_NOTES}
${EXISTING_CONFIG_NOTE}

## 2. 架构

<!-- 待补充：补充具体的项目架构描述 -->

这是一个 Python 项目。常见模式：

- CLI 工具 / 库 / Web 服务（Flask / FastAPI / Django）
- 包管理通过 pip / Poetry / uv
- 虚拟环境隔离依赖

## 3. 目录规范

${DIRECTORY_TREE}

<!-- 待补充：补充各目录的用途说明 -->

## 4. 核心约定

- **风格**：遵循 PEP 8（通过 ruff 或 black 执行），行长 ≤ 120。
- **类型标注**：所有函数签名必须有类型标注，通过 mypy 或 pyright 检查。
- **测试**：使用 pytest + fixtures，核心逻辑覆盖率 ≥ 80%。
- **依赖**：在 `requirements.txt` 或 `pyproject.toml` 中锁定版本。

## 5. 人工智能协作规则

- 编辑前必须先读相关模块。
- 先写计划再编码（≥ 3 个文件的改动必须先写计划）。
- 禁止手动修改 lock 文件。
- 提交信息格式：`type(scope): 描述`（遵循约定式提交）。
- 遇到歧义时先问，不要假设；如有多种方案，展示取舍后让用户选。
- 只改需求相关的代码——diff 里每一行都要能追溯到用户的要求。
- 只清理自己引入的废代码；发现的无关问题留注释提醒，不擅自修改。
- 能简单就不复杂——没被要求的抽象、灵活性、配置化不要加。

## 6. 禁令

- 禁止提交 `.env`、凭证或密钥。
- 禁止无注释使用 `# type: ignore`。
- 禁止使用裸 `except:` —— 必须指定异常类型。
